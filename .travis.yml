language: python
python:
  - "3.9"

addons:
  chrome: stable

branches:
  only:
    - main

gitinstall:
  - pip install pipenv
  - pipenv install --dev

before_script:
  - wget https://chromedriver.storage.googleapis.com/LATEST_RELEASE
  - wget https://chromedriver.storage.googleapis.com/`cat LATEST_RELEASE`/chromedriver_linux64.zip
  - sudo unzip chromedriver_linux64.zip -d /home/travis/build/Anca6889/P10_deployment/

env:
  global:
    - DJANGO_SETTINGS_MODULE="P8_pur_beurre.settings.travis"

services:
  - postgresql

script:
  - python manage.py test